{% extends 'layout/clients/base_clients.html.twig' %}


{% block title %}Register
{% endblock %}

{% block body %}
	{% for flash_error in app.flashes('verify_email_error') %}
		<div class="alert alert-danger" role="alert">{{ flash_error }}</div>
	{% endfor %}

	<main>
		<section>
			<h1 class="h1-history text-center text-white fw-bold display-6 text-uppercase">inscription</h1>
		</section>

		<section>
			<div class="d-flex justify-content-center">
				<div class="registration-content">
					{{ form_start(registrationForm, {'attr': { 'novalidate': 'novalidate' }}) }}
					{{ form_row(registrationForm.firstname) }}
					{{ form_row(registrationForm.lastname) }}
					{{ form_row(registrationForm.phone) }}
					{{ form_row(registrationForm.email) }}
					{{ form_row(registrationForm.plainPassword) }}

					<div class="mb-3 mt-4">
						<p class="text-center text-white text-uppercase">se connecter en tant que :</p>
						<div class="d-flex justify-content-between justify-content-md-evenly">
							<div class="checkbox-driver me-3">
								{{ form_row(registrationForm.isDriver) }}
							</div>
							<div class="checkbox-passenger me-3">
								{{ form_row(registrationForm.isPassenger) }}
							</div>
						</div>
						{# {{ form_row(registrationForm.agreeTerms) }} #}
						<div class="d-flex justify-content-center">
							<button class="button-connect btn btn-lg mt-4 text-uppercase fw-bold" type="submit">valider</button>
						</div>
					</div>
					{{ form_end(registrationForm) }}

					{# 
					            <form>
					                <div class="form-group mb-4">
					                    <div class="d-flex justify-content-center">
					                        <label class="text-white text-uppercase" for="exampleInputEmail1">pseudo</label>
					                    </div>
					                        <input type="text" class="input-pseudo-register form-control mb-3" id="text" aria-describedby="emailHelp" placeholder="" />
					                    <div class="d-flex justify-content-center">
					                        <label class="text-white text-uppercase" for="exampleInputEmail1">Email</label>
					                    </div>
					                        <input type="email" class="input-email-register form-control" id="exampleInputEmail1" aria-describedby="emailHelp" placeholder="" />
					                    <small id="emailHelp" class="form-text">We'll never share your email with anyone else.</small>
					                </div>
					                <div class="form-group mb-4">
					                    <div class="d-flex justify-content-center">
					                        <label class="text-white text-uppercase" for="exampleInputPassword1">mot de passe</label>
					                    </div>
					                        <input type="password" class="input-mdp-register form-control mb-3" id="exampleInputPassword1" placeholder="" />
					                    <div class="d-flex justify-content-center">
					                        <label class="text-white text-uppercase" for="exampleInputPassword1">confirmer mot de passe</label>
					                    </div>
					                    <input type="password" class="input-confirm_mdp-register form-control" id="exampleInputPassword1" placeholder="" />
					                </div>
					                <div class="d-flex justify-content-center">
					                    <p class="text-white text-uppercase">se connecter en tant que :</p>
					                </div>
					                <div class="d-flex justify-content-between justify-content-md-evenly">
					                    <div class="checkbox-driver d-flex align-items-center flex-no-wrap">
					                        <input type="checkbox" name="_chauffeur" id="_chauffeur" class="me-2" />
					                        <label class="text-uppercase text-white" for="_chauffeur">chauffeur</label>
					                    </div>
					                    <div class="checkbox-passenger d-flex align-items-center flex-no-wrap">
					                        <input type="checkbox" name="_passager" id="_passager" class="me-2" />
					                        <label class="text-uppercase text-white" for="_passager">passager</label>
					                    </div>
					                </div>
					                <div class="d-flex justify-content-center">
					                    <button class="button-connect btn btn-lg mt-4 text-uppercase fw-bold" type="submit">valider
					                    </button>
					                </div>
					            </form> #}
				</div>
			</div>

			{# {{ form_errors(registrationForm) }}
			
			            {{ form_start(registrationForm) }}
			            <div class="input-email">
			                {{ form_row(registrationForm.email) }}
			            </div>
			            <div class="input-password">
			                {{ form_row(registrationForm.plainPassword, {
			                    label: 'Password'
			                }) }}
			            </div>
			                {{ form_row(registrationForm.agreeTerms) }}
			
			                <button type="submit" class="btn">Register</button>
			            {{ form_end(registrationForm) }} #}
		</main>
	{% endblock %}
